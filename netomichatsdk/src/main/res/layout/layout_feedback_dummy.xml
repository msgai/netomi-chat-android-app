<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F8F8F8">

    <TextView
        android:id="@+id/descriptionTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Netomi is a leading AI-driven customer experience platform that empowers businesses to deliver exceptional support and engagement."
        android:textSize="16sp"
        android:textColor="#333333"
        android:lineSpacingExtra="4dp"
        android:layout_marginEnd="8dp"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/thumbDownButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/thumps_down_unselected"
            android:background="@drawable/bg_rounded_corner"
            android:contentDescription="Thumb down"
            android:padding="2dp"/>

        <ImageButton
            android:id="@+id/thumbUpButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/thumps_up_selected"
            android:background="@drawable/bg_rounded_corner"
            android:contentDescription="Thumb up"
            android:padding="2dp"/>
    </LinearLayout>

</LinearLayout>
